<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.ck.dao.SuccessSeckillDao">
    <select id="getModelByPrimaryId" resultType="SuccessSeckill">
        select

        from seckill.seckill s inner join seckill.success_killed sk on s.seckill_id = sk.seckill_id
        where s.
    </select>
</mapper>